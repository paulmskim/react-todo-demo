import React from 'react';

const Header = () => (
  <div className="header">
    <h1 className="title">React Todo Demo</h1>
    <p className="desc">
      Enter a new todo to add to the todo list.
      Click on a todo to toggle its completed state.
    </p>
  </div>
);

export default Header;
